# Java OOP — Interview Preparation

A structured collection of **Java Object-Oriented Programming (OOP) notes** focused on understanding core concepts and preparing for technical interviews.

The notes cover Java OOP fundamentals, important interview concepts, common Java-specific behaviors, and frequently asked questions.

---

## 📚 Topics Covered

### 1. Classes & Objects
- Class vs Object
- State, Identity, and Behavior
- Object creation using `new`
- References
- Reference assignment
- Primitive vs Reference types
- Parameters vs Arguments
- Java is Pass-by-Value

### 2. `this`, `final` & `finalize()`
- `this` keyword
- `final` variable
- `final` method
- `final` class
- `final` reference behavior
- `finalize()` and its deprecation

### 3. Constructors
- Default constructor
- Parameterized constructor
- Constructor execution
- Constructor with inheritance
- `super()` and constructor chaining
- Constructor execution order

### 4. Access Modifiers
- `public`
- `protected`
- default
- `private`
- Same-package vs different-package access
- Protected access with inheritance

### 5. Static
- Static variables
- Static methods
- Static blocks
- Static nested classes
- Static method hiding
- Static interface methods
- `static` vs instance members

### 6. Inheritance
- `extends`
- Single inheritance
- Superclass and subclass
- `super`
- Constructor chaining
- `final` with inheritance
- Parent reference → child object

### 7. Polymorphism
- Compile-time polymorphism
- Runtime polymorphism
- Method overloading
- Method overriding
- Dynamic method dispatch
- Static method hiding
- Field hiding vs method overriding

### 8. Abstraction
- Abstract classes
- Abstract methods
- Interfaces
- Abstract class vs Interface
- Multiple interfaces
- Default methods
- Static interface methods
- Interface conflict resolution

### 9. Packages
- Package structure
- Namespaces
- Access control
- Classpath basics

### 10. Enums
- Enum declaration
- Enum constants
- `values()`
- `ordinal()`
- `valueOf()`
- Enum constructors
- Enum with interfaces

### 11. Singleton Design Pattern
- Singleton implementation
- Private constructor
- Static instance
- `getInstance()`
- Thread-safety considerations
- Enum Singleton

### 12. Interview Questions
Includes rapid-fire questions covering:
- Multiple inheritance
- Static method overriding
- Overloading vs overriding
- Abstract class constructors
- `super()`
- `final` references
- Interface state
- Runtime polymorphism
- Field vs method polymorphism

---

## 🔑 Core OOP Concepts

The repository focuses on the major concepts behind Java OOP:

```text
              OOP
               |
      ---------------------
      |         |         |
 Encapsulation Inheritance Polymorphism
               |
          Abstraction

```
The notes also cover Java-specific concepts that are commonly asked in interviews.

## 🎯 Purpose

These notes are intended for:

- Java interview preparation
- Campus placements
- Technical interviews
- Java fundamentals revision
- Understanding OOP concepts before learning advanced Java
- Quick revision before coding interviews

---

## 🧠 Interview Focus

While studying, focus especially on understanding the difference between:

- **Class vs Object**
- **`this` vs `super`**
- **`final` vs `finally` vs `finalize`**
- **Overloading vs Overriding**
- **Compile-time vs Runtime Polymorphism**
- **Abstract Class vs Interface**
- **Static method hiding vs Method overriding**
- **Reference type vs Object type**
- **Field hiding vs Method overriding**
- **`public` vs `protected` vs default vs `private`**
- **Java Pass-by-Value**
- **Constructor chaining**
- **Single vs Multiple Inheritance**

---

## 📁 Repository Structure

```text
Java-OOP/
│
├── Java-OOP-Interview-Notes.md
└── README.md

**One thing I'd add to the actual `.md` later:** a short dedicated **"Encapsulation"** section, b
